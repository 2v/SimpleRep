module.exports = {
    name: 'repinfo',
    args: true,
    description: 'Check the role names and rep thresholds associated with each',
    guildOnly: true,
    async execute(message, args) {

    }
}