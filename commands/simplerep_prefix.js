module.exports = {
    name: 'simplerep_prefix',
    args: true,
    admin: true,
    usage: '<prefix>',
    description: 'Set the desired server prefix to use for SimpleRep. The default prefix is \`!\`',
    guildOnly: true,
    async execute(message, args) {

    }
}